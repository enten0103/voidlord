name: Build

on:
  push:
    branches: [ "master" ]
  workflow_dispatch:

jobs:
  build-android:
    name: Build Android
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'
          
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          
      - name: Decode Keystore
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
        run: |
          if [ -n "$ANDROID_KEYSTORE_BASE64" ]; then
            echo "Decoding keystore..."
            echo "$ANDROID_KEYSTORE_BASE64" | base64 --decode > android/app/upload-keystore.jks
            
            echo "Creating key.properties..."
            echo "keyAlias=$ANDROID_KEY_ALIAS" > android/key.properties
            echo "keyPassword=$ANDROID_KEY_PASSWORD" >> android/key.properties
            echo "storePassword=$ANDROID_KEYSTORE_PASSWORD" >> android/key.properties
            echo "storeFile=upload-keystore.jks" >> android/key.properties
          else
            echo "Warning: Secret ANDROID_KEYSTORE_BASE64 not found."
            echo "Build may fail if signing config requires these files."
          fi

      - name: Get dependencies
        run: flutter pub get

      - name: Build APK
        run: flutter build apk --release

      - name: Rename APKs
        run: |
          COMMIT_HASH=$(git rev-parse --short HEAD)
          echo "SHORT_SHA=$COMMIT_HASH" >> $GITHUB_ENV
          
          cd build/app/outputs/flutter-apk/
          
          # 先处理 Universal APK
          if [ -f app-release.apk ]; then
            mv app-release.apk "VoidLord-Android-Universal-${COMMIT_HASH}.apk"
          fi
          
          # 再处理 Split APKs
          for file in app-*-release.apk; do
            if [[ "$file" == "app-release.apk" ]]; then continue; fi
            
            if [ -f "$file" ]; then
              arch=${file#app-}
              arch=${arch%-release.apk}
              mv "$file" "VoidLord-Android-${arch}-${COMMIT_HASH}.apk"
            fi
          done
          
          ls -la

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: VoidLord-Android-${{ env.SHORT_SHA }}
          path: build/app/outputs/flutter-apk/*.apk

  build-windows:
    name: Build Windows
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          
      - name: Get dependencies
        run: flutter pub get

      - name: Build Windows
        run: flutter build windows --release

      - name: Install Inno Setup
        run: choco install innosetup

      - name: Compile Installer
        run: iscc windows/installer/installer.iss

      - name: Rename Installer
        run: |
          $hash = git rev-parse --short HEAD
          echo "SHORT_SHA=$hash" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          
          $installer = Get-ChildItem build\installer\*.exe | Select-Object -First 1
          if ($installer) {
              Write-Host "Renaming $($installer.FullName) to VoidLord-Windows-x64-$hash.exe"
              Rename-Item $installer.FullName "VoidLord-Windows-x64-$hash.exe"
          } else {
              Write-Host "No installer found in build\installer\"
              Get-ChildItem build\installer\
          }

      - name: Upload Installer
        uses: actions/upload-artifact@v4
        with:
          name: VoidLord-Windows-${{ env.SHORT_SHA }}
          path: build/installer/*.exe
