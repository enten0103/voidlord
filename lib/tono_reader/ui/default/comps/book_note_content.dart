import 'package:flutter/widgets.dart';

class BookNoteContent extends StatelessWidget {
  const BookNoteContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("笔记");
  }
}
